version: '3'
services:
  qsl-pdf-generator:
    build: ./
    container_name: 'qsl-pdf-generator'
    command: /bin/bash -c "cd /work && poetry install && poetry run python src/app.py"
    volumes:
      - ./:/work
      - ./output:/output
